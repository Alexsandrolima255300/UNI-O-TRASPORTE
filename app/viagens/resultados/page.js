"use client";import{useEffect,useState}from"react";import{useSearchParams}from"next/navigation";import Link from"next/link";import{supabase}from"../../../lib/supabase";
export default function Resultados(){const q=useSearchParams(),[trips,setTrips]=useState([]),[loading,setLoading]=useState(true),[msg,setMsg]=useState(""),o=q.get("origem")||"",d=q.get("destino")||"",date=q.get("data")||"";useEffect(()=>{(async()=>{let x=supabase.from("trips").select("id,departure_date,departure_time,arrival_time,price,status,routes!inner(origin,destination),buses(name,model)").eq("departure_date",date).neq("status","cancelled").order("departure_time");if(o)x=x.ilike("routes.origin","%"+o+"%");if(d)x=x.ilike("routes.destination","%"+d+"%");const{data,error}=await x;if(error)setMsg(error.message);else setTrips(data||[]);setLoading(false)})()},[date,o,d]);return <main className="page-shell"><header className="topbar"><Link className="brand" href="/">UNIÃO<span>TRANSPORTES</span></Link><nav><Link href="/viagens">Nova busca</Link><Link href="/conta">Minha conta</Link><Link href="/login">Entrar</Link></nav></header><section className="results-page"><div className="section-head"><span className="eyebrow">RESULTADOS</span><h1>{o||"Origem"} → {d||"Destino"}</h1><p>{date?new Date(date+"T12:00:00").toLocaleDateString("pt-BR"):""} · {trips.length} opção(ões)</p></div>{loading?<div className="empty-info"><strong>Consultando viagens...</strong></div>:msg?<div className="empty-info"><strong>Erro ao consultar</strong><span>{msg}</span></div>:trips.length===0?<div className="empty-info"><strong>Nenhuma viagem disponível.</strong><span>O administrador precisa cadastrar uma viagem para essa rota e data.</span></div>:<div className="trip-list">{trips.map(t=><article className="trip-card" key={t.id}><div className="trip-time"><strong>{t.departure_time.slice(0,5)}</strong><span>{t.routes.origin}</span></div><div className="trip-track"><i></i><span>{t.arrival_time?t.arrival_time.slice(0,5):"—"}</span><i></i></div><div className="trip-time"><strong>{t.arrival_time?t.arrival_time.slice(0,5):"—"}</strong><span>{t.routes.destination}</span></div><div className="trip-meta"><span>{t.buses?.name||"Ônibus"}</span><strong>R$ {Number(t.price).toLocaleString("pt-BR",{minimumFractionDigits:2})}</strong><Link className="button primary small" href={"/viagens/assentos?trip="+t.id}>Escolher</Link></div></article>)}</div>}</section></main>}